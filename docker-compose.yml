# GreenMail IMAP/SMTP test server for the integration tests in
# crates/mail-core/tests/imap.rs and crates/mail-core/tests/smtp.rs.
#
#   docker compose up -d
#   TEGAMI_GREENMAIL_HOST=localhost cargo test -p mail-core
#
# The same image and options are used by CI (see .github/workflows/ci.yml).
services:
  greenmail:
    image: greenmail/standalone:2.1.13
    ports:
      - "3143:3143"
    environment:
      GREENMAIL_OPTS: "-Dgreenmail.setup.test.all -Dgreenmail.hostname=0.0.0.0 -Dgreenmail.users=user:pass@localhost"